services:
  mysql-usuarios:
    image: mysql:8
    container_name: mysql_c
    environment:
      MYSQL_ROOT_PASSWORD: utec
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"
    networks:
      - network
    
  adminer:
    image: adminer
    container_name: adminer_c
    ports:
      - "8080:8080"
    networks:
      - network

volumes:
  mysql_data:

networks:
  network:
